apiVersion: loadtest.artillery.io/v1alpha1
kind: LoadTest
metadata:
  name: basic-loadtest
  namespace: default
spec:
  # Add fields here
  count: 5
  environment: dev
  testScript:
    config:
      configMap: basic-test-script
